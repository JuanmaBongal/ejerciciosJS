/**Crear un array de tamaño 10 y que guardará números enteros introducidos
 *  por teclado. Tras introducirlos todos, imprimirá cada índice junto 
 * con el valor al que corresponda.
*/

console.clear();
const prompt = require("prompt-sync")();

let numbers = Number(prompt("Introduce numero entero: "));
let guardar = [];
const lengtharray=10;
for (let i=0; i<lengtharray; i++){
    console.log(`${imprimenumeros}`);
}

function imprimenumeros (){
    for(let i=0; i<lengtharray; i++){
        
    }
}